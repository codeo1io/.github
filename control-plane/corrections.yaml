# Fleet corrections ledger — fro-bot/.github corrections.yaml pattern.
# Lifecycle: record → reconfirm (before reconfirm_due) → retire | supersede.
# reconfirm_due dates are alarmable: fleet-status flags any entry whose due
# date passes without a recorded reconfirmation.
version: 1
entries:
  - id: kanban-canon-v2
    what: Kanban OWNERS canon v2 — ground truth ~/.hermes/kanban/logs/<task>.log; dead-assignee wedge recovery = reassign default+ready+reset consecutive_failures
    recorded: 2026-09-17
    recorded_by: fleet-adoption
    source: ~/.hermes/kanban/attachments/t_acd6a2e8/OWNERS.md
    reconfirm_due: 2026-12-17
    status: active
  - id: settings-sync-plan-limit
    what: GitHub free plan silently refuses allow_auto_merge + branch protection on private repos; auto-merge stripped from drift set, protection opt-in file governs
    recorded: 2026-09-20
    recorded_by: fleet-adoption
    source: codeo1io/.github common-settings.yaml
    reconfirm_due: 2026-12-20
    status: active
  - id: secret-scanning-sticky-disabled
    what: magic-hermes + openai-embedding-proxy revert secret_scanning enablement (account-level); leak-scanning cover is NOT uniform — magic-hermes runs the gitleaks sentinel (reusable workflow @main, verified live 2026-09-21) but openai-embedding-proxy has NO sentinel workflow and NO secret scanning (.github/workflows contains only ci.yml). Cross-repo sentinel adoption tracked as roadmap rm-016 deferred half.
    recorded: 2026-09-21
    recorded_by: fleet-adoption
    amended: "2026-09-21 claim corrected after live audit (gh api repos/codeo1io/openai-embedding-proxy/contents/.github/workflows -> ci.yml only, no sentinel/gitleaks reference; conductor run 92e960046f14 cycle-2 B3). 2026-09-22 cycle-4 batch D3: leak-scanning cover extended — .github itself now self-scans on push/PR/weekly via leak-sentinel-self.yaml (reusable sentinel + SHA-pinned gitleaks-action v3 + pinned config checkout), so the leak-config owner repo is no longer unscanned; coverage now magic-hermes (caller @main) + .github (self); openai-embedding-proxy still has NO sentinel and NO secret scanning — remains the rm-016 deferred half"
    source: gh api security_and_analysis (PATCH reverts to disabled) + gh api contents .github/workflows per repo
    reconfirm_due: 2026-12-21
    status: active
